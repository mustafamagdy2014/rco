import { NumberSuffexPipe } from './number-suffex.pipe';

describe('NumberSuffexPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberSuffexPipe();
    expect(pipe).toBeTruthy();
  });
});
